---
name: learned
description: Use only when the user invokes /learned or explicitly asks to record a durable finding, reusable lesson, or project learning in memory.
---

# Learned

Follow the project `AGENTS.md` memory directives. This skill is a project-local shortcut; it does not override project instructions.

## Steps

1. Add the finding to `memory/learnings.md` under the most appropriate section.
2. Add supporting context or evidence paths, not bulky raw output.
3. If the stable `<node>` name is not known, ask the user before creating or writing a new node-specific note.
4. Add a short entry to today's project note, preferring `memory/notes/YYYY-MM-DD-<node>.md` for new notes.
5. If the learning changes the current framing, blocker, or next action, update `memory/index.md`.
