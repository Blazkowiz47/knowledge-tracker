interface:
  display_name: "Sync"
  short_description: "Role-aware knowledge-base synchronization"
  default_prompt: "Use $sync to select this node's publishing or coordinator workflow and synchronize the knowledge base safely."
