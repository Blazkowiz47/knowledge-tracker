interface:
  display_name: "Sync"
  short_description: "Pull, ingest, commit, and push KB changes."
  default_prompt: "Use $sync to pull the knowledge base, ingest project memories, and push KB changes."
