interface:
  display_name: "Jira Rovo MCP"
  short_description: "Use Jira through Atlassian Rovo MCP."
  default_prompt: "Use $jira-rovo-mcp to draft or publish Jira updates from project memory through Atlassian Rovo MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "atlassian"
      description: "Atlassian Rovo remote MCP server with Jira tools"
      transport: "streamable_http"
      url: "https://mcp.atlassian.com/v1/mcp/authv2"
